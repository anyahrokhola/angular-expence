import { IconBackgroundColorPipe } from '../../icon-background-color.pipe';

describe('IconBackgroundColorPipe', () => {
  it('create an instance', () => {
    const pipe = new IconBackgroundColorPipe();
    expect(pipe).toBeTruthy();
  });
});
